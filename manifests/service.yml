apiVersion: v1
kind: Service
metadata:
  name: ping-pong-svc
  namespace: log-app
spec:
  type: NodePort
  selector:
    app: ping-pong-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3500